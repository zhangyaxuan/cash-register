require 'spec_helper'

describe Cash::Register do
  it 'has a version number' do
    expect(Cash::Register::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
